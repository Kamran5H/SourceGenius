{
  "name": "source-genius",
  "private": true,
  "description": "Test harness for the Source Genius pipeline (brand-finder-extension, amazon-playwright-scraper, sg-vpn-controller). See README.md for how the four components fit together.",
  "scripts": {
    "test": "node --test test/*.test.js"
  }
}
